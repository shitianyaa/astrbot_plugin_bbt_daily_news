repos:
  - repo: local
    hooks:
      - id: render-options-tests
        name: T2I render options regression tests
        entry: python3 -m unittest discover -s tests -p test_render_options.py
        language: system
        pass_filenames: false
